---
const stats = [
  { value: 90, suffix: '%', label: 'Noise Ratio', description: 'of online content will be AI-generated by 2026' },
  { value: 15, suffix: 'M+', label: 'AI Images/Day', description: 'synthetic visuals flooding the internet daily' },
  { value: 24, suffix: '/7', label: 'Bot Activity', description: 'synthetic content generation never stops' },
  { value: 1, suffix: '', label: 'Solution', description: 'unified detection for text, images & video' },
];
---

<section id="problem" class="section problem">
  <div class="container">
    <div class="problem-header text-center slide-up">
      <h2>THE LOOMING AI DELUGE</h2>
      <p class="problem-description">
        By 2026, 90% of online content will be synthetically generated — text, images, videos, all of it. 
        The internet was built for connection, not simulation. Without verification, you can't trust 
        what you read, see, or watch anymore.
      </p>
    </div>
    
    <div class="stats-grid grid grid-4 stagger-children">
      {stats.map(stat => (
        <div class="stat-card card">
          <div class="stat-value">
            <span 
              class="stat-number" 
              data-counter 
              data-target={stat.value} 
              data-suffix={stat.suffix}
            >
              0{stat.suffix}
            </span>
          </div>
          <div class="stat-label">{stat.label}</div>
          <p class="stat-description">{stat.description}</p>
        </div>
      ))}
    </div>
  </div>
  
  <div class="problem-visual">
    <div class="wave-line" data-parallax="0.1"></div>
    <div class="wave-line wave-line-2" data-parallax="0.15"></div>
  </div>
</section>

<style>
  .problem {
    position: relative;
    overflow: hidden;
  }
  
  .problem-header {
    margin-bottom: var(--space-4xl);
  }
  
  .problem-header h2 {
    margin-bottom: var(--space-lg);
  }
  
  .problem-description {
    max-width: 700px;
    margin: 0 auto;
    font-size: 1.125rem;
    line-height: 1.8;
  }
  
  .stats-grid {
    position: relative;
    z-index: 1;
  }
  
  .stat-card {
    text-align: center;
    padding: var(--space-2xl) var(--space-lg);
  }
  
  .stat-value {
    margin-bottom: var(--space-md);
  }
  
  .stat-number {
    font-family: var(--font-serif);
    font-size: clamp(3rem, 8vw, 4.5rem);
    color: var(--color-primary);
    line-height: 1;
    letter-spacing: -0.04em;
  }
  
  .stat-label {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-text);
    margin-bottom: var(--space-sm);
  }
  
  .stat-description {
    font-size: 0.875rem;
    color: var(--color-text-dim);
    margin: 0;
    line-height: 1.6;
  }
  
  .problem-visual {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 200px;
    pointer-events: none;
    overflow: hidden;
  }
  
  .wave-line {
    position: absolute;
    bottom: 0;
    left: -10%;
    width: 120%;
    height: 100px;
    background: linear-gradient(90deg, 
      transparent, 
      var(--color-primary-dim), 
      var(--color-primary),
      var(--color-primary-dim),
      transparent
    );
    mask-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 1200 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M0 50 Q 300 0 600 50 T 1200 50 V 100 H 0 Z' fill='black'/%3E%3C/svg%3E");
    -webkit-mask-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 1200 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M0 50 Q 300 0 600 50 T 1200 50 V 100 H 0 Z' fill='black'/%3E%3C/svg%3E");
    mask-size: cover;
    -webkit-mask-size: cover;
    opacity: 0.1;
  }
  
  .wave-line-2 {
    height: 60px;
    bottom: 20px;
    opacity: 0.05;
  }
  
  @media (max-width: 768px) {
    .stats-grid {
      gap: var(--space-md);
    }
    
    .stat-card {
      padding: var(--space-xl) var(--space-md);
    }
  }
</style>

