---
const base = import.meta.env.BASE_URL;
---

<section class="manifesto-summary" id="manifesto">
  <div class="container">
    <div class="manifesto-card">
      <span class="badge">Our Manifesto</span>
      <blockquote>
        <p>The internet is drowning in synthetic content — and most people can't tell what's real anymore. We're not anti-AI. We just believe human connection matters, and authenticity shouldn't be a luxury.</p>
        <p>We're building the tools to fight back. Not to censor, but to answer one simple question: <em>did a human actually write this?</em></p>
      </blockquote>
      <a href={`${base}manifesto/`} class="manifesto-link">
        Read the full manifesto
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .manifesto-summary {
    padding: var(--space-4xl) 0;
  }
  
  .manifesto-card {
    max-width: 800px;
    margin: 0 auto;
    padding: var(--space-3xl);
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    text-align: center;
  }
  
  .manifesto-card .badge {
    margin-bottom: var(--space-xl);
  }
  
  blockquote {
    margin: 0 0 var(--space-xl);
  }
  
  blockquote p {
    font-family: var(--font-serif);
    font-size: clamp(1.125rem, 2vw, 1.25rem);
    line-height: 1.7;
    color: var(--color-text-muted);
    margin-bottom: var(--space-md);
  }
  
  blockquote p:last-child {
    margin-bottom: 0;
  }
  
  blockquote em {
    color: var(--color-primary);
    font-style: italic;
  }
  
  .manifesto-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--color-text-muted);
    letter-spacing: 0.02em;
    transition: color var(--transition-fast);
  }
  
  .manifesto-link:hover {
    color: var(--color-primary);
  }
  
  .manifesto-link svg {
    transition: transform var(--transition-fast);
  }
  
  .manifesto-link:hover svg {
    transform: translateX(4px);
  }
</style>

